<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Diwali Chit Funds 2025</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: #333;
            min-height: 100vh;
        }

        /* Header */
        .header {
            background: rgba(255, 255, 255, 0.95);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            position: sticky;
            top: 0;
            z-index: 1000;
            padding: 15px 0;
        }

        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            font-size: 28px;
            color: #ff6b00;
        }

        .logo-text {
            font-size: 26px;
            font-weight: 700;
            background: linear-gradient(45deg, #ff6b00, #ff3d00);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .auth-buttons {
            display: flex;
            gap: 15px;
        }

        .auth-btn {
            padding: 10px 25px;
            border-radius: 30px;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            border: none;
            text-decoration: none;
            display: inline-block;
            text-align: center;
        }

        .login-btn {
            background: linear-gradient(45deg, #6a11cb, #2575fc);
            color: white;
        }

        .login-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(106, 17, 203, 0.4);
        }

        .signup-btn {
            background: linear-gradient(45deg, #ff6b00, #ff3d00);
            color: white;
        }

        .signup-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(255, 107, 0, 0.4);
        }

        /* Hero Banner with your image */
        .hero-banner {
            background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), 
                        url('data:image/png;base64,');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            color: white;
            padding: 80px 20px;
            text-align: center;
            border-bottom: 5px solid #ff6b00;
            min-height: 500px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        .hero-title {
            font-size: 42px;
            font-weight: 800;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .hero-subtitle {
            font-size: 22px;
            max-width: 800px;
            margin: 0 auto 30px;
            line-height: 1.6;
            background: rgba(0, 0, 0, 0.6);
            padding: 20px;
            border-radius: 10px;
        }

        .highlight {
            color: #ffcc00;
            font-weight: 700;
        }

        /* Rest of your CSS remains exactly the same */
        /* Main Content */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .section {
            background: white;
            border-radius: 20px;
            padding: 40px;
            margin-bottom: 40px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .section-title {
            text-align: center;
            font-size: 32px;
            color: #333;
            margin-bottom: 30px;
            position: relative;
            padding-bottom: 15px;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 4px;
            background: linear-gradient(45deg, #ff6b00, #ff3d00);
            border-radius: 2px;
        }

        /* Features */
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            margin-top: 30px;
        }

        .feature-card {
            background: #f8f9fa;
            border-radius: 15px;
            padding: 25px;
            text-align: center;
            transition: all 0.3s ease;
            border-left: 5px solid #ff6b00;
        }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }

        .feature-icon {
            font-size: 40px;
            color: #ff6b00;
            margin-bottom: 15px;
        }

        .feature-title {
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 10px;
            color: #333;
        }

        .feature-desc {
            color: #666;
            line-height: 1.6;
        }

        /* Plans Table */
        .plans-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 30px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
        }

        .plans-table th {
            background: linear-gradient(45deg, #6a11cb, #2575fc);
            color: white;
            padding: 18px;
            text-align: center;
            font-weight: 600;
            font-size: 18px;
        }

        .plans-table td {
            padding: 16px;
            text-align: center;
            border-bottom: 1px solid #eee;
            font-size: 17px;
        }

        .plans-table tr:nth-child(even) {
            background: #f8f9fa;
        }

        .plans-table tr:hover {
            background: #e3f2fd;
        }

        .plan-amount {
            color: #ff6b00;
            font-weight: 700;
            font-size: 20px;
        }

        .plan-total {
            color: #2575fc;
            font-weight: 700;
            font-size: 20px;
        }

        /* Cards */
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 30px;
            margin-top: 30px;
        }

        .plan-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            transition: all 0.4s ease;
            position: relative;
        }

        .plan-card:hover {
            transform: translateY(-10px) scale(1.02);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .card-header {
            background: linear-gradient(45deg, #ff6b00, #ff3d00);
            color: white;
            padding: 25px;
            text-align: center;
        }

        .card-amount {
            font-size: 42px;
            font-weight: 800;
            margin-bottom: 5px;
        }

        .card-period {
            font-size: 18px;
            opacity: 0.9;
        }

        .card-body {
            padding: 30px 25px;
            text-align: center;
        }

        .card-details {
            list-style: none;
            margin-bottom: 25px;
        }

        .card-details li {
            padding: 10px 0;
            border-bottom: 1px dashed #eee;
            color: #666;
            font-size: 16px;
        }

        .card-details li:last-child {
            border-bottom: none;
        }

        .card-total {
            font-size: 28px;
            font-weight: 700;
            color: #2575fc;
            margin-bottom: 20px;
        }

        .card-btn {
            background: linear-gradient(45deg, #6a11cb, #2575fc);
            color: white;
            border: none;
            padding: 14px 30px;
            border-radius: 30px;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            width: 100%;
        }

        .card-btn:hover {
            background: linear-gradient(45deg, #2575fc, #6a11cb);
            transform: translateY(-2px);
        }

        /* Payment Methods - UPDATED */
        .payment-methods {
            display: flex;
            justify-content: center;
            gap: 30px;
            flex-wrap: wrap;
            margin-top: 30px;
        }

        .payment-method {
            background: white;
            border-radius: 15px;
            padding: 20px;
            width: 120px;
            height: 120px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .payment-method:hover {
            transform: translateY(-5px) scale(1.1);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
        }

        .payment-method img {
            max-width: 100%;
            max-height: 60px;
            width: auto;
            height: auto;
        }

        /* Footer */
        .footer {
            background: rgba(0, 0, 0, 0.9);
            color: white;
            padding: 60px 20px 30px;
            margin-top: 60px;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
        }

        .footer-section h3 {
            font-size: 22px;
            margin-bottom: 20px;
            color: #ffcc00;
        }

        .footer-section p {
            color: #bbb;
            line-height: 1.8;
            margin-bottom: 10px;
        }

        .contact-info {
            font-size: 18px;
            margin-top: 15px;
        }

        .contact-info i {
            color: #ff6b00;
            margin-right: 10px;
            width: 20px;
        }

        .copyright {
            text-align: center;
            padding-top: 30px;
            margin-top: 40px;
            border-top: 1px solid #444;
            color: #aaa;
            font-size: 14px;
        }

        /* WhatsApp Button */
        .whatsapp-float {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: #25D366;
            color: white;
            width: 70px;
            height: 70px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            box-shadow: 0 6px 20px rgba(37, 211, 102, 0.5);
            z-index: 1000;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .whatsapp-float:hover {
            transform: scale(1.1);
            box-shadow: 0 10px 25px rgba(37, 211, 102, 0.7);
        }

        /* Modals */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            z-index: 2000;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .modal.active {
            display: flex;
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .modal-content {
            background: white;
            border-radius: 20px;
            width: 100%;
            max-width: 500px;
            overflow: hidden;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            animation: slideUp 0.4s ease;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .modal-header {
            background: linear-gradient(45deg, #6a11cb, #2575fc);
            color: white;
            padding: 25px;
            text-align: center;
        }

        .modal-header h2 {
            font-size: 28px;
            margin-bottom: 5px;
        }

        .modal-body {
            padding: 30px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }

        .form-control {
            width: 100%;
            padding: 15px;
            border: 2px solid #ddd;
            border-radius: 10px;
            font-size: 16px;
            transition: border 0.3s;
        }

        .form-control:focus {
            border-color: #6a11cb;
            outline: none;
        }

        .form-actions {
            display: flex;
            gap: 15px;
            margin-top: 30px;
        }

        .btn {
            flex: 1;
            padding: 16px;
            border: none;
            border-radius: 10px;
            font-weight: 600;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-primary {
            background: linear-gradient(45deg, #ff6b00, #ff3d00);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 15px rgba(255, 107, 0, 0.4);
        }

        .btn-secondary {
            background: #f1f1f1;
            color: #333;
        }

        .btn-secondary:hover {
            background: #e0e0e0;
        }

        .modal-close {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(0, 0, 0, 0.5);
            color: white;
            border: none;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            font-size: 20px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Login Prompt */
        .login-prompt {
            text-align: center;
            padding: 50px 20px;
            background: linear-gradient(45deg, #ffcc00, #ff9900);
            border-radius: 20px;
            margin: 40px 0;
            color: white;
        }

        .login-prompt h2 {
            font-size: 32px;
            margin-bottom: 15px;
        }

        .login-prompt p {
            font-size: 18px;
            margin-bottom: 25px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .header-container {
                flex-direction: column;
                gap: 15px;
            }
            
            .hero-title {
                font-size: 32px;
            }
            
            .hero-subtitle {
                font-size: 18px;
                padding: 15px;
            }
            
            .section {
                padding: 30px 20px;
            }
            
            .section-title {
                font-size: 28px;
            }
            
            .plans-table th,
            .plans-table td {
                padding: 12px 8px;
                font-size: 14px;
            }
            
            .cards-grid {
                grid-template-columns: 1fr;
            }
            
            .payment-methods {
                gap: 20px;
            }
            
            .payment-method {
                width: 100px;
                height: 100px;
                padding: 15px;
            }
            
            .form-actions {
                flex-direction: column;
            }
            
            .whatsapp-float {
                width: 60px;
                height: 60px;
                font-size: 30px;
                bottom: 20px;
                right: 20px;
            }
            
            .hero-banner {
                padding: 60px 20px;
                min-height: 400px;
            }
        }

        @media (max-width: 480px) {
            .auth-buttons {
                flex-direction: column;
                width: 100%;
            }
            
            .auth-btn {
                width: 100%;
            }
            
            .hero-title {
                font-size: 26px;
            }
            
            .hero-subtitle {
                font-size: 16px;
                padding: 12px;
            }
            
            .section-title {
                font-size: 24px;
            }
            
            .payment-methods {
                gap: 15px;
            }
            
            .payment-method {
                width: 80px;
                height: 80px;
                padding: 10px;
            }
            
            .payment-method img {
                max-height: 45px;
            }
            
            .modal-content {
                margin: 10px;
            }
            
            .hero-banner {
                padding: 40px 15px;
                min-height: 350px;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="header-container">
            <div class="logo">
                <div class="logo-icon">🎆</div>
                <div class="logo-text">Diwali Chit Funds 2025</div>
            </div>
            <div class="auth-buttons">
                @auth
                    <a href="{{ url('/member') }}" class="auth-btn login-btn">Dashboard</a>
                @else
                    <button class="auth-btn login-btn" id="headerLoginBtn">Login</button>
                    <button class="auth-btn signup-btn" id="headerSignupBtn">Sign Up</button>
                @endauth
            </div>
        </div>
    </header>

    <!-- Hero Banner -->
    <section class="hero-banner">
        <h1 class="hero-title">Diwali Crackers Fund 2025</h1>
        <p class="hero-subtitle">
            Pay monthly installments and get <span class="highlight">1.5 months free bonus</span> with 
            <span class="highlight">60% discount</span> on booking. Enjoy festive savings with guaranteed delivery before Diwali!
        </p>
    </section>

    <!-- Main Content -->
    <div class="container">
        <!-- Features Section -->
        <section class="section">
            <h2 class="section-title">Why Choose Our Fund?</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">💰</div>
                    <h3 class="feature-title">Big Savings</h3>
                    <p class="feature-desc">Get 60% discount on booking with 1.5 months free bonus on timely payments.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🚚</div>
                    <h3 class="feature-title">Free Delivery</h3>
                    <p class="feature-desc">Free shipping for Tamil Nadu, Pondicherry & Bengaluru (pickup points available).</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">📅</div>
                    <h3 class="feature-title">Flexible Payments</h3>
                    <p class="feature-desc">Pay before 10th of every month. Missed payments still get 60% discount.</p>
                </div>
            </div>
        </section>

        <!-- Plans Section -->
        <section class="section">
            <h2 class="section-title">Investment Plans</h2>
            
            <div class="cards-grid" id="plansContainer">
                <!-- Plans will be generated by JavaScript -->
            </div>

            <div style="margin-top: 40px;">
                <h3 style="text-align: center; margin-bottom: 20px; color: #333;">Detailed Plan Comparison</h3>
                <table class="plans-table">
                    <thead>
                        <tr>
                            <th>Monthly Installment</th>
                            <th>Total Months</th>
                            <th>Total + Bonus</th>
                            <th>Final Amount</th>
                        </tr>
                    </thead>
                    <tbody id="plansTableBody">
                        <!-- Table rows will be generated by JavaScript -->
                    </tbody>
                </table>
            </div>
        </section>

        <!-- Login Prompt -->
        <div class="login-prompt">
            @auth
                <h2>Welcome Back, {{ auth()->user()->name }}!</h2>
                <p>Access your dashboard to view your chit fund details.</p>
                <a href="{{ url('/member') }}" class="auth-btn login-btn" style="font-size: 18px; padding: 15px 40px;">Go to Dashboard</a>
            @else
                <h2>Already a Member?</h2>
                <p>Login to view your subscriptions, payment history, and account details.</p>
                <button class="auth-btn login-btn" id="mainLoginBtn" style="font-size: 18px; padding: 15px 40px;">Login to Your Account</button>
            @endauth
        </div>

        <!-- Payment Methods - UPDATED -->
        <section class="section">
            <h2 class="section-title">Accepted Payment Methods</h2>
            <div class="payment-methods">
                <div class="payment-method">
                    <img src="https://static.cdnlogo.com/logos/p/92/phonepe_800.png" alt="PhonePe">
                </div>
                <div class="payment-method">
                    <!-- Google Pay - WORKING URL -->
                    <img src="https://upload.wikimedia.org/wikipedia/commons/f/f2/Google_Pay_Logo.svg" alt="Google Pay">
                </div>
                <div class="payment-method">
                    <img src="https://upload.wikimedia.org/wikipedia/commons/4/42/Paytm_logo.png" alt="Paytm">
                </div>
                <div class="payment-method">
                    <img src="https://upload.wikimedia.org/wikipedia/commons/e/e1/UPI-Logo-vector.svg" alt="UPI">
                </div>
            </div>
        </section>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <div class="footer-section">
                <h3>Contact Us</h3>
                <div class="contact-info">
                    <p><i class="fas fa-phone"></i> 9597731397</p>
                    <p><i class="fas fa-phone"></i> 8122482884</p>
                    <p><i class="fas fa-clock"></i> Mon-Sat: 9 AM - 8 PM</p>
                </div>
            </div>
            <div class="footer-section">
                <h3>Fund Details</h3>
                <p>Fund Period: October 2025 - July 2026</p>
                <p>Booking: 10 Aug 2026 with 60% discount</p>
                <p>Dispatch: End of Aug 2026</p>
            </div>
            <div class="footer-section">
                <h3>Website</h3>
                <p><i class="fas fa-globe"></i> www.aishwaryamtraders.com</p>
                <p><i class="fas fa-map-marker-alt"></i> Free shipping: TN, Pondicherry, Bengaluru</p>
            </div>
        </div>
        <div class="copyright">
            © 2025 Diwali Chit Funds. All rights reserved. | Designed with ❤️ for happy Diwali celebrations
        </div>
    </footer>

    <!-- WhatsApp Float -->
    <a href="https://wa.me/919597731397?text=Hi%2C%20I'm%20interested%20in%20Diwali%20Crackers%20Fund%202025" 
       class="whatsapp-float" target="_blank">
       <i class="fab fa-whatsapp"></i>
    </a>

    <!-- Login Modal -->
    <div class="modal" id="loginModal">
        <div class="modal-content">
            <button class="modal-close" id="closeLoginModal">×</button>
            <div class="modal-header">
                <h2>Member Login</h2>
                <p>Enter your credentials to access your dashboard</p>
            </div>
            <div class="modal-body">
                <form id="loginForm" method="POST" action="{{ route('custom.login') }}">
                    @csrf
                    <div class="form-group">
                        <label for="loginEmail">Email Address</label>
                        <input type="email" name="login_identifier" id="loginEmail" class="form-control" placeholder="Enter your email" required>
                    </div>
                    <div class="form-group">
                        <label for="loginPassword">Password</label>
                        <input type="password" name="password" id="loginPassword" class="form-control" placeholder="Enter your password" required>
                    </div>
                    <div class="form-actions" style="flex-direction: row-reverse;">
                        <button type="submit" class="btn btn-primary">Login Now</button>
                        <button type="button" class="btn btn-secondary" id="cancelLogin">Cancel</button>
                    </div>
                    @if($errors->has('login_identifier'))
                        <div style="color: #ff3d00; margin-top: 15px; font-size: 14px; text-align: center; background: #fff1f0; padding: 10px; border-radius: 8px; border: 1px solid #ffa39e;">
                            {{ $errors->first('login_identifier') }}
                        </div>
                    @endif
                </form>
            </div>
        </div>
    </div>

    <!-- Signup Modal -->
    <div class="modal" id="signupModal">
        <div class="modal-content">
            <button class="modal-close" id="closeSignupModal">×</button>
            <div class="modal-header">
                <h2>Create Account</h2>
                <p>Join our Diwali Chit Fund with just your email</p>
            </div>
            <div class="modal-body">
                <form id="signupForm" method="POST" action="{{ route('custom.signup') }}">
                    @csrf
                    <div class="form-group">
                        <label for="signupEmail">Email Address</label>
                        <input type="email" name="email" id="signupEmail" class="form-control" placeholder="Enter your email" required>
                    </div>
                    <div class="form-group">
                        <label for="signupPassword">Create Password</label>
                        <input type="password" name="password" id="signupPassword" class="form-control" placeholder="Create a strong password" required>
                    </div>
                    <div class="form-actions" style="flex-direction: row-reverse;">
                        <button type="submit" class="btn btn-primary">Sign Up Now</button>
                        <button type="button" class="btn btn-secondary" id="cancelSignup">Cancel</button>
                    </div>
                    @if($errors->any())
                        <div style="color: #ff3d00; margin-top: 15px; font-size: 14px; text-align: center; background: #fff1f0; padding: 10px; border-radius: 8px; border: 1px solid #ffa39e;">
                            {{ $errors->first() }}
                        </div>
                    @endif
                </form>
            </div>
        </div>
    </div>

    <!-- Plan Enquiry Modal -->
    <div class="modal" id="enquiryModal">
        <div class="modal-content">
            <button class="modal-close" id="closeEnquiryModal">×</button>
            <div class="modal-header">
                <h2>Plan Enquiry</h2>
                <p id="selectedPlanText"></p>
            </div>
            <div class="modal-body">
                <form id="enquiryForm">
                    <div class="form-group">
                        <label for="enquiryName">Full Name</label>
                        <input type="text" id="enquiryName" class="form-control" placeholder="Enter your full name" required>
                    </div>
                    <div class="form-group">
                        <label for="enquiryMobile">Mobile Number</label>
                        <input type="tel" id="enquiryMobile" class="form-control" placeholder="Enter 10-digit mobile" maxlength="10" required>
                    </div>
                    <div class="form-group">
                        <label for="enquiryEmail">Email Address</label>
                        <input type="email" id="enquiryEmail" class="form-control" placeholder="Enter email address" required>
                    </div>
                    <div class="form-group">
                        <label for="enquiryLocation">Location</label>
                        <input type="text" id="enquiryLocation" class="form-control" placeholder="Enter your city/area" required>
                    </div>
                    <div class="form-actions">
                        <button type="button" class="btn btn-secondary" id="cancelEnquiry">Cancel</button>
                        <button type="submit" class="btn btn-primary">Submit Enquiry</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script>
        // Plan Data
        const plans = [
            { amount: 300, months: 10, bonus: 450, total: 3450 },
            { amount: 400, months: 10, bonus: 600, total: 4600 },
            { amount: 500, months: 10, bonus: 750, total: 5750 },
            { amount: 750, months: 10, bonus: 1125, total: 8625 },
            { amount: 1000, months: 10, bonus: 1500, total: 11500 },
            { amount: 1250, months: 10, bonus: 1875, total: 14375 },
            { amount: 1500, months: 10, bonus: 2250, total: 17250 },
            { amount: 2000, months: 10, bonus: 3000, total: 23000 }
        ];

        // DOM Elements
        const plansContainer = document.getElementById('plansContainer');
        const plansTableBody = document.getElementById('plansTableBody');
        const loginModal = document.getElementById('loginModal');
        const signupModal = document.getElementById('signupModal');
        const enquiryModal = document.getElementById('enquiryModal');
        const selectedPlanText = document.getElementById('selectedPlanText');
        let selectedPlan = null;

        // Initialize the page
        document.addEventListener('DOMContentLoaded', function() {
            generatePlanCards();
            generatePlanTable();
            setupEventListeners();
        });

        // Generate Plan Cards
        function generatePlanCards() {
            plansContainer.innerHTML = '';
            plans.forEach(plan => {
                const card = document.createElement('div');
                card.className = 'plan-card';
                card.innerHTML = `
                    <div class="card-header">
                        <div class="card-amount">₹${plan.amount}</div>
                        <div class="card-period">Per Month</div>
                    </div>
                    <div class="card-body">
                        <ul class="card-details">
                            <li>${plan.months} Months Plan</li>
                            <li>Total: ₹${plan.amount * plan.months}</li>
                            <li>Bonus: ₹${plan.bonus}</li>
                            <li>Pay before 10th every month</li>
                        </ul>
                        <div class="card-total">₹${plan.total}</div>
                        <button class="card-btn" data-plan='${JSON.stringify(plan)}'>
                            Enquire Now
                        </button>
                    </div>
                `;
                plansContainer.appendChild(card);
            });
        }

        // Generate Plan Table
        function generatePlanTable() {
            plansTableBody.innerHTML = '';
            plans.forEach(plan => {
                const row = document.createElement('tr');
                row.innerHTML = `
                    <td class="plan-amount">₹${plan.amount}</td>
                    <td>${plan.months}</td>
                    <td>₹${plan.amount * plan.months} + ₹${plan.bonus}</td>
                    <td class="plan-total">₹${plan.total}</td>
                `;
                plansTableBody.appendChild(row);
            });
        }

        // Setup Event Listeners
        function setupEventListeners() {
            // Login buttons
            const headerLoginBtn = document.getElementById('headerLoginBtn');
            const mainLoginBtn = document.getElementById('mainLoginBtn');
            const headerSignupBtn = document.getElementById('headerSignupBtn');

            if (headerLoginBtn) headerLoginBtn.addEventListener('click', () => openModal(loginModal));
            if (mainLoginBtn) mainLoginBtn.addEventListener('click', () => openModal(loginModal));
            if (headerSignupBtn) headerSignupBtn.addEventListener('click', () => openModal(signupModal));
            
            // Close buttons
            document.getElementById('closeLoginModal').addEventListener('click', () => closeModal(loginModal));
            document.getElementById('closeSignupModal').addEventListener('click', () => closeModal(signupModal));
            document.getElementById('closeEnquiryModal').addEventListener('click', () => closeModal(enquiryModal));
            
            // Cancel buttons
            document.getElementById('cancelLogin').addEventListener('click', () => closeModal(loginModal));
            document.getElementById('cancelSignup').addEventListener('click', () => closeModal(signupModal));
            document.getElementById('cancelEnquiry').addEventListener('click', () => closeModal(enquiryModal));
            
            // Plan enquiry buttons
            document.addEventListener('click', function(e) {
                if (e.target.classList.contains('card-btn')) {
                    const planData = JSON.parse(e.target.getAttribute('data-plan'));
                    selectedPlan = planData;
                    selectedPlanText.textContent = `₹${planData.amount} X ${planData.months} MONTHS - Total: ₹${planData.total}`;
                    openModal(enquiryModal);
                }
            });
            
            // Form submissions
            document.getElementById('loginForm').addEventListener('submit', handleLogin);
            document.getElementById('signupForm').addEventListener('submit', handleSignup);
            document.getElementById('enquiryForm').addEventListener('submit', handleEnquiry);
            
            // Close modal when clicking outside
            [loginModal, signupModal, enquiryModal].forEach(modal => {
                if (modal) {
                    modal.addEventListener('click', function(e) {
                        if (e.target === modal) {
                            closeModal(modal);
                        }
                    });
                }
            });

            // Explicitly handle Enter key for all forms to ensure submission
            const forms = ['loginForm', 'signupForm', 'enquiryForm'];
            forms.forEach(formId => {
                const form = document.getElementById(formId);
                if (form) {
                    const inputs = form.querySelectorAll('input');
                    inputs.forEach(input => {
                        input.addEventListener('keydown', function(e) {
                            if (e.key === 'Enter') {
                                // For enquiry form, we handle it manually
                                if (formId === 'enquiryForm') {
                                    e.preventDefault();
                                    handleEnquiry(e);
                                } else {
                                    // For login/signup, let the submit event fire normally
                                    // but we can explicitly trigger click on submit button if needed
                                }
                            }
                        });
                    });
                }
            });
        }

        // Modal Functions
        function openModal(modal) {
            modal.classList.add('active');
            document.body.style.overflow = 'hidden';
        }

        function closeModal(modal) {
            modal.classList.remove('active');
            document.body.style.overflow = 'auto';
            // Reset forms
            if (modal === enquiryModal) {
                document.getElementById('enquiryForm').reset();
                selectedPlan = null;
            }
        }
        // Form Handlers
        function handleLogin(e) {
            const email = document.getElementById('loginEmail').value;
            const password = document.getElementById('loginPassword').value;
            
            if (!email || !password) {
                e.preventDefault();
                alert('Please fill in all fields');
            }
        }

        function handleSignup(e) {
            const email = document.getElementById('signupEmail').value;
            const password = document.getElementById('signupPassword').value;
            
            if (!email || !password) {
                e.preventDefault();
                alert('Please fill in all fields');
            }
        }

        function handleEnquiry(e) {
            e.preventDefault();
            const name = document.getElementById('enquiryName').value;
            const mobile = document.getElementById('enquiryMobile').value;
            const email = document.getElementById('enquiryEmail').value;
            const location = document.getElementById('enquiryLocation').value;
            
            // Validation
            if (!name || !mobile || !email || !location) {
                alert('Please fill in all fields');
                return;
            }
            
            if (mobile.length !== 10) {
                alert('Please enter a valid 10-digit mobile number');
                return;
            }
            
            if (!selectedPlan) {
                alert('No plan selected');
                return;
            }
            
            // Create WhatsApp message
            const message = `*Diwali Chit Fund 2025 Enquiry*%0A%0A` +
                          `*Plan:* ₹${selectedPlan.amount} X ${selectedPlan.months} Months%0A` +
                          `*Total Amount:* ₹${selectedPlan.total}%0A` +
                          `*Name:* ${name}%0A` +
                          `*Mobile:* ${mobile}%0A` +
                          `*Email:* ${email}%0A` +
                          `*Location:* ${location}%0A%0A` +
                          `I'm interested in this plan. Please contact me with more details.`;
            
            // Open WhatsApp
            window.open(`https://wa.me/919597731397?text=${encodeURIComponent(message)}`, '_blank');
            
            // Show confirmation
            alert('Thank you for your enquiry! Opening WhatsApp to send your details.');
            
            // Close modal
            closeModal(enquiryModal);
        }

        // Add some sample data for demo
        window.addEventListener('load', function() {
            // Auto-fill demo data (for testing only)
            if (window.location.href.includes('demo=true')) {
                document.getElementById('loginEmail').value = 'demo@example.com';
                document.getElementById('loginPassword').value = 'demo123';
                document.getElementById('signupName').value = 'John Doe';
                document.getElementById('signupMobile').value = '9876543210';
                document.getElementById('signupEmail').value = 'john@example.com';
                document.getElementById('signupPassword').value = 'password123';
            }
        });

     
        document.querySelectorAll('input[type="tel"]').forEach(input => {
            input.addEventListener('input', function() {
                this.value = this.value.replace(/\D/g, '').slice(0, 10);
            });
        });

        @if ($errors->any())
            @if($errors->has('login_identifier'))
                openModal(loginModal);
            @elseif($errors->has('email') || $errors->has('password'))
                openModal(signupModal);
            @endif
        @endif
    </script>
</body>
</html>